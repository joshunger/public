---
- hosts: localhost
  gather_facts: no
  roles:
    - {role: 'brew', tags: 'brew'}
    - {role: 'docker', tags: 'docker'}
    - {role: 'vscode', tags: 'vscode'}
    # - pip
    # - user
    # - dock
    # - php
    # - sublime
    # - xbox
    # - vscode
    # - zsh

# ansible-playbook --ask-become-pass --become-user "$USER" provision/playbook.yml --tags brew